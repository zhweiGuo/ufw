#!/bin/sh -e

case "$1" in
    remove)
        # Unload ufw (will only happen if enabled)
        if [ -x /usr/lib/ufw/ufw-init ]; then
            /usr/lib/ufw/ufw-init stop || true
        fi
        ;;
esac

#DEBHELPER#
